PORT=3000

# Upstash Redis (same DB used by /api/user and /api/guild/events)
UPSTASH_REDIS_REST_URL_GUILD=
UPSTASH_REDIS_REST_TOKEN_GUILD=

# Optional: shared secret for server-side notify endpoint
DISCORD_NOTIFY_SECRET=
